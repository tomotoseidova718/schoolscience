function sampleCode() {
  // This is a random TS code snippet.
  let variable: string = "Hello, TypeScript!";
  
  // Example of using variables in a function or loop.
  for (let i = 0; i < 10; i++) {
    console.log(`Number ${i + 1}: ${variable}`);
  }
}
